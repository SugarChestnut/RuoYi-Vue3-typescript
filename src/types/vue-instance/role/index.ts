export interface SelectUserInstance {
    show: () => void;
}

export interface ScrollPaneInstance {
    moveToTarget: (currentTag: string) => void;
}

export interface EditRoleInstance {
    show: () => void;
}
